{
    "name": "hotdog-counter",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "migrate": "wrangler d1 execute hotdog-db --file=./migrations/0001_init.sql"
    }
}